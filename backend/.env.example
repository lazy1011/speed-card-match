# Port the server listens on (Railway sets this automatically)
PORT=4000

# Frontend origin for Socket.io CORS — set to your Vercel deployment URL in production
# e.g. https://speed-card-match.vercel.app
FRONTEND_URL=http://localhost:3000
